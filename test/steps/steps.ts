import { Given, Then, When } from "@cucumber/cucumber";
import assert from "assert";

Given("a variable set to {int}", function (int) {
  // Given('a variable set to {float}', function (float) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

When("I increment the variable by {int}", function (int) {
  // When('I increment the variable by {float}', function (float) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Then("the variable should contain {int}", function (int) {
  // Then('the variable should contain {float}', function (float) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});
